# Copy to .env.local and adjust as needed.
# The Next.js dev server rewrites /api/* to this upstream backend.
# ATLAS-020 ships the proxy only; no API calls are wired yet.
API_UPSTREAM=http://127.0.0.1:8000
